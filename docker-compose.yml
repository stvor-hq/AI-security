version: "3.9"

services:
  relay:
    build: .
    ports:
      - "${RELAY_PORT:-8787}:8787"
    environment:
      PORT: "${RELAY_PORT:-8787}"
      STVOR_APP_TOKEN: "${RELAY_TOKEN}"
      # Pass STVOR_APP_TOKEN explicitly at runtime:
      #   RELAY_TOKEN=your-secret docker compose up
