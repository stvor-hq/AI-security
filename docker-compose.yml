version: '3.8'
services:
  stvor-demo:
    build: .
    image: stvor-cloud:demo
    tty: true
    stdin_open: true
    env_file:
      - .env
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    command: ["bun", "start:demo"]
    restart: "no"
